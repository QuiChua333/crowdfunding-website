import { useDispatch, useSelector } from "react-redux"
import { setMessageBox } from "~/redux/slides/GlobalApp";

const  MessageBox  = {
    
    // show: function({title, content, contentCancel, contentOK}){
    //     dispatch(setMessageBox({title, content, contentCancel, contentOK, isShow: true}))
    // }
}
export default MessageBox